## Materials Project `material_id` search bug

- Mission: search for `mp-2815` and return its downloaded structure path.
- The LLM correctly called `search_materials_project` with `{"material_id": "mp-2815"}`, but VASPilot returned unrelated `mp-3346412` because `struct_tools.py` documented `material_id` without forwarding it to the Materials Project API.
- Fix: after initializing `search_params`, map a string `material_id` to `search_params["material_ids"] = [material_id]` (and pass lists through unchanged), then restart the MCP server.
- The separate delegated-output JSON warning was fixed in `server_listener.py` by treating Markdown/plain text as `raw_output` and parsing only structured output. The warning was absent in the retest.
